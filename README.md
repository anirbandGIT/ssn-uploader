# SSN Uploader

Monorepo app using:

- React (web)
- Node + Express (api)
- PostgreSQL
- MinIO
- Turborepo + pnpm

## Setup

```bash
pnpm install
docker compose up -d
pnpm run server
pnpm run client
```
